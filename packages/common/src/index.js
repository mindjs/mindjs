const {
  Module,
  Inject,
  Injectable,
  Optional,
} = require('./decorators');

const CommonModule = require('./common.module');

module.exports = {
  Module,
  Inject,
  Injectable,
  Optional,

  CommonModule,
};
