const {
  Optional: _Optional,
  Inject: _Inject,
  InjectionToken,
  Injector,
  forwardRef,
  resolveForwardRef,
  ReflectiveInjector,
  ResolvedReflectiveFactory,
  ReflectiveKey,
} = require('injection-js');

module.exports = {
  // re-export `injection-js`'s entities
  _Optional,
  _Inject,
  InjectionToken,
  Injector,
  forwardRef,
  resolveForwardRef,
  ReflectiveInjector,
  ResolvedReflectiveFactory,
  ReflectiveKey,
};
