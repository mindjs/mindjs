const {
  Inject: _Inject,
  InjectionToken,
  Injector,
  forwardRef,
  resolveForwardRef,
  ReflectiveInjector,
  ResolvedReflectiveFactory,
  ReflectiveKey,
} = require('injection-js');

module.exports = {
  // re-export `injection-js`'s entities
  _Inject,
  InjectionToken,
  Injector,
  forwardRef,
  resolveForwardRef,
  ReflectiveInjector,
  ResolvedReflectiveFactory,
  ReflectiveKey,
};
